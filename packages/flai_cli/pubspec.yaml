name: flai_cli
description: CLI tool for installing FlAI AI chat components into Flutter projects.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  args: ^2.7.0
  mason: ^0.1.0
  path: ^1.9.0
  yaml: ^3.1.0
  yaml_edit: ^2.2.0

dev_dependencies:
  test: ^1.25.0
