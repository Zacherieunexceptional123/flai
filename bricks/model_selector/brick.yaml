name: model_selector
description: Dropdown selector for switching between AI models with descriptions
version: 0.1.0
environment:
  mason: ">=0.1.0 <1.0.0"
vars:
  output_dir:
    type: string
    description: Output directory for generated files
    default: flai
    prompt: "Where are your FlAI files?"
