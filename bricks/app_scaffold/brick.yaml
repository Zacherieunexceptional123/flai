name: app_scaffold
description: Production-ready Flutter app shell with auth, chat, settings, and profile screens — just hook up your backend
version: 0.1.0
environment:
  mason: ">=0.1.0 <1.0.0"
vars:
  output_dir:
    type: string
    description: Output directory for generated files
    default: flai
    prompt: "Where are your FlAI files?"
